{
  "name": "smart-erp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dash": "npm --prefix dashboard run dev",
    "dev:dash": "npm --prefix dashboard run dev",
    "build:dash": "npm --prefix dashboard run build",
    "dev:all": "npx concurrently -n \"BACKEND,FRONTEND,DASHBOARD\" -c \"bgGreen.bold,bgCyan.bold,bgBlue.bold\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix dashboard run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
