import pkg from '../../../package.json';

export const APP_NAME = 'Monsur Ali Travels';
export const APP_VERSION = pkg.version || '03.04.17';
export const APP_TAGLINE = `Smart ERP v${APP_VERSION}`;

export default {
  APP_NAME,
  APP_VERSION,
  APP_TAGLINE,
};
