// Re-exported from shared — single source of truth for Universal Modal styling.
export { Modal, default } from '@shared/components/ui/Modal';
export { UnifiedModal, UnifiedModalHeader, UnifiedModalFooter } from '@shared/components/common/UnifiedModal';

